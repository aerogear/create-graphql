import Relay from 'react-relay';

export default class <%= name %>EditMutation extends Relay.Mutation {
  static fragments = {
    <%= camelCasedName %>: () => Relay.QL`
      fragment on <%= name %> {
        id
      }
    `,
  };

  getMutation() {
    return Relay.QL`mutation {
      <%= name %>Edit
    }`;
  }

  getVariables() {
    const {
      <%= camelCasedName %>: {
        id,
      },
      // Todo add more mutation input fields here
    } = this.props;

    return {
      id,
      // TODO add more mutation input fields here
    };
  }

  getFatQuery() {
    return Relay.QL`
      fragment on <%= name %>EditPayload {
        <%= camelCasedName %>
      }
    `;
  }

  getConfigs() {
    return [{
      type: 'FIELDS_CHANGE',
      fieldIDs: {
      <%= camelCasedName %>: this.props.<%= camelCasedName %>.id,
      },
    }];
  }
}
