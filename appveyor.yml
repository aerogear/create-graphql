environment:
  matrix:
    - node_version: "6"
    - node_version: "4"

install:
  - ps: Install-Product node $env:nodejs_version
  - yarn install
  - yarn bootstrap

build: off

test_script:
  - node --version
  - yarn --version
  - yarn lint
  - yarn test -- --runInBand

cache:
  - node_modules
  - "%LOCALAPPDATA%\\Yarn"