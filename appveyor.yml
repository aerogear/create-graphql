environment:
  nodejs_version: "6"
  nodejs_version: "4"

cache:
  - "%LOCALAPPDATA%\\Yarn"

install:
  - ps: Install-Product node $env:nodejs_version
  - yarn install

test_script:
  - node --version
  - yarn --version
  - yarn lint
  - yarn test -- --runInBand